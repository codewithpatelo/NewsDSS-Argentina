/*
 Copyright (C) Federico Zivolo 2017
 Distributed under the MIT License (license terms are at http://opensource.org/licenses/MIT).
 */
(function (e, t) {
  typeof exports === 'object' && typeof module !== 'undefined' ? module.exports = t() : typeof define === 'function' && define.amd ? define(t) : e.Popper = t();
}(this, () => {
  function e(e) {
    return e && {}.toString.call(e) === '[object Function]';
  }

  function t(e, t) {
    if (e.nodeType !== 1) return [];
    const o = window.getComputedStyle(e, null);
    return t ? o[t] : o;
  }

  function o(e) {
    return e.nodeName === 'HTML' ? e : e.parentNode || e.host;
  }

  function n(e) {
    if (!e || ['HTML', 'BODY', '#document'].indexOf(e.nodeName) !== -1) return window.document.body;
    let i = t(e),
      r = i.overflow,
      p = i.overflowX,
      s = i.overflowY;
    return /(auto|scroll)/.test(r + s + p) ? e : n(o(e));
  }

  function r(e) {
    let o = e && e.offsetParent,
      i = o && o.nodeName;
    return i && i !== 'BODY' && i !== 'HTML' ? ['TD', 'TABLE'].indexOf(o.nodeName) !== -1 && t(o, 'position') === 'static' ? r(o) : o : window.document.documentElement;
  }

  function p(e) {
    const t = e.nodeName;
    return t !== 'BODY' && (t === 'HTML' || r(e.firstElementChild) === e);
  }

  function s(e) {
    return e.parentNode === null ? e : s(e.parentNode);
  }

  function d(e, t) {
    if (!e || !e.nodeType || !t || !t.nodeType) return window.document.documentElement;
    let o = e.compareDocumentPosition(t) & Node.DOCUMENT_POSITION_FOLLOWING,
      i = o ? e : t,
      n = o ? t : e,
      a = document.createRange();
    a.setStart(i, 0), a.setEnd(n, 0);
    const f = a.commonAncestorContainer;
    if (e !== f && t !== f || i.contains(n)) return p(f) ? f : r(f);
    const l = s(e);
    return l.host ? d(l.host, t) : d(e, s(t).host);
  }

  function a(e) {
    let t = arguments.length > 1 && void 0 !== arguments[1] ? arguments[1] : 'top',
      o = t === 'top' ? 'scrollTop' : 'scrollLeft',
      i = e.nodeName;
    if (i === 'BODY' || i === 'HTML') {
      let n = window.document.documentElement,
        r = window.document.scrollingElement || n;
      return r[o];
    }
    return e[o];
  }

  function f(e, t) {
    let o = arguments.length > 2 && void 0 !== arguments[2] && arguments[2],
      i = a(t, 'top'),
      n = a(t, 'left'),
      r = o ? -1 : 1;
    return e.top += i * r, e.bottom += i * r, e.left += n * r, e.right += n * r, e;
  }

  function l(e, t) {
    let o = t === 'x' ? 'Left' : 'Top',
      i = o == 'Left' ? 'Right' : 'Bottom';
    return +e[`border${o}Width`].split('px')[0] + +e[`border${i}Width`].split('px')[0];
  }

  function m(e, t, o, i) {
    return _(t[`offset${e}`], o[`client${e}`], o[`offset${e}`], ie() ? o[`offset${e}`] + i[`margin${e === 'Height' ? 'Top' : 'Left'}`] + i[`margin${e === 'Height' ? 'Bottom' : 'Right'}`] : 0);
  }

  function h() {
    let e = window.document.body,
      t = window.document.documentElement,
      o = ie() && window.getComputedStyle(t);
    return {
      height: m('Height', e, t, o),
      width: m('Width', e, t, o),
    };
  }

  function c(e) {
    return se({}, e, {
      right: e.left + e.width,
      bottom: e.top + e.height,
    });
  }

  function g(e) {
    let o = {};
    if (ie()) {
      try {
        o = e.getBoundingClientRect();
        let i = a(e, 'top'),
          n = a(e, 'left');
        o.top += i, o.left += n, o.bottom += i, o.right += n;
      } catch (e) {}
    } else o = e.getBoundingClientRect();
    let r = {
        left: o.left,
        top: o.top,
        width: o.right - o.left,
        height: o.bottom - o.top,
      },
      p = e.nodeName === 'HTML' ? h() : {},
      s = p.width || e.clientWidth || r.right - r.left,
      d = p.height || e.clientHeight || r.bottom - r.top,
      f = e.offsetWidth - s,
      m = e.offsetHeight - d;
    if (f || m) {
      const g = t(e);
      f -= l(g, 'x'), m -= l(g, 'y'), r.width -= f, r.height -= m;
    }
    return c(r);
  }

  function u(e, o) {
    let i = ie(),
      r = o.nodeName === 'HTML',
      p = g(e),
      s = g(o),
      d = n(e),
      a = t(o),
      l = +a.borderTopWidth.split('px')[0],
      m = +a.borderLeftWidth.split('px')[0],
      h = c({
        top: p.top - s.top - l,
        left: p.left - s.left - m,
        width: p.width,
        height: p.height,
      });
    if (h.marginTop = 0, h.marginLeft = 0, !i && r) {
      let u = +a.marginTop.split('px')[0],
        b = +a.marginLeft.split('px')[0];
      h.top -= l - u, h.bottom -= l - u, h.left -= m - b, h.right -= m - b, h.marginTop = u, h.marginLeft = b;
    }
    return (i ? o.contains(d) : o === d && d.nodeName !== 'BODY') && (h = f(h, o)), h;
  }

  function b(e) {
    let t = window.document.documentElement,
      o = u(e, t),
      i = _(t.clientWidth, window.innerWidth || 0),
      n = _(t.clientHeight, window.innerHeight || 0),
      r = a(t),
      p = a(t, 'left'),
      s = {
        top: r - o.top + o.marginTop,
        left: p - o.left + o.marginLeft,
        width: i,
        height: n,
      };
    return c(s);
  }

  function y(e) {
    const i = e.nodeName;
    return i === 'BODY' || i === 'HTML' ? !1 : t(e, 'position') === 'fixed' || y(o(e));
  }

  function w(e, t, i, r) {
    let p = {
        top: 0,
        left: 0,
      },
      s = d(e, t);
    if (r === 'viewport') p = b(s);
    else {
      let a;
      r === 'scrollParent' ? (a = n(o(e)), a.nodeName === 'BODY' && (a = window.document.documentElement)) : r === 'window' ? a = window.document.documentElement : a = r;
      const f = u(a, s);
      if (a.nodeName === 'HTML' && !y(s)) {
        let l = h(),
          m = l.height,
          c = l.width;
        p.top += f.top - f.marginTop, p.bottom = m + f.top, p.left += f.left - f.marginLeft, p.right = c + f.left;
      } else p = f;
    }
    return p.left += i, p.top += i, p.right -= i, p.bottom -= i, p;
  }

  function v(e) {
    let t = e.width,
      o = e.height;
    return t * o;
  }

  function E(e, t, o, i, n) {
    const r = arguments.length > 5 && void 0 !== arguments[5] ? arguments[5] : 0;
    if (e.indexOf('auto') === -1) return e;
    let p = w(o, i, r, n),
      s = {
        top: {
          width: p.width,
          height: t.top - p.top,
        },
        right: {
          width: p.right - t.right,
          height: p.height,
        },
        bottom: {
          width: p.width,
          height: p.bottom - t.bottom,
        },
        left: {
          width: t.left - p.left,
          height: p.height,
        },
      },
      d = Object.keys(s).map(e => se({
        key: e,
      }, s[e], {
        area: v(s[e]),
      })).sort((e, t) => t.area - e.area),
      a = d.filter((e) => {
        let t = e.width,
          i = e.height;
        return t >= o.clientWidth && i >= o.clientHeight;
      }),
      f = a.length > 0 ? a[0].key : d[0].key,
      l = e.split('-')[1];
    return f + (l ? `-${l}` : '');
  }

  function x(e, t, o) {
    const i = d(t, o);
    return u(o, i);
  }

  function O(e) {
    let t = window.getComputedStyle(e),
      o = parseFloat(t.marginTop) + parseFloat(t.marginBottom),
      i = parseFloat(t.marginLeft) + parseFloat(t.marginRight),
      n = {
        width: e.offsetWidth + i,
        height: e.offsetHeight + o,
      };
    return n;
  }

  function L(e) {
    const t = {
      left: 'right',
      right: 'left',
      bottom: 'top',
      top: 'bottom',
    };
    return e.replace(/left|right|bottom|top/g, e => t[e]);
  }

  function S(e, t, o) {
    o = o.split('-')[0];
    let i = O(e),
      n = {
        width: i.width,
        height: i.height,
      },
      r = ['right', 'left'].indexOf(o) !== -1,
      p = r ? 'top' : 'left',
      s = r ? 'left' : 'top',
      d = r ? 'height' : 'width',
      a = r ? 'width' : 'height';
    return n[p] = t[p] + t[d] / 2 - i[d] / 2, n[s] = o === s ? t[s] - i[a] : t[L(s)], n;
  }

  function T(e, t) {
    return Array.prototype.find ? e.find(t) : e.filter(t)[0];
  }

  function C(e, t, o) {
    if (Array.prototype.findIndex) {
      return e.findIndex(e => e[t] === o);
    }
    const i = T(e, e => e[t] === o);
    return e.indexOf(i);
  }

  function N(t, o, i) {
    const n = void 0 === i ? t : t.slice(0, C(t, 'name', i));
    return n.forEach((t) => {
      t.function && console.warn('`modifier.function` is deprecated, use `modifier.fn`!');
      const i = t.function || t.fn;
      t.enabled && e(i) && (o.offsets.popper = c(o.offsets.popper), o.offsets.reference = c(o.offsets.reference), o = i(o, t));
    }), o;
  }

  function k() {
    if (!this.state.isDestroyed) {
      let e = {
        instance: this,
        styles: {},
        attributes: {},
        flipped: !1,
        offsets: {},
      };
      e.offsets.reference = x(this.state, this.popper, this.reference), e.placement = E(this.options.placement, e.offsets.reference, this.popper, this.reference, this.options.modifiers.flip.boundariesElement, this.options.modifiers.flip.padding), e.originalPlacement = e.placement, e.offsets.popper = S(this.popper, e.offsets.reference, e.placement), e.offsets.popper.position = 'absolute', e = N(this.modifiers, e), this.state.isCreated ? this.options.onUpdate(e) : (this.state.isCreated = !0, this.options.onCreate(e));
    }
  }

  function W(e, t) {
    return e.some((e) => {
      let o = e.name,
        i = e.enabled;
      return i && o === t;
    });
  }

  function B(e) {
    for (let t = [!1, 'ms', 'Webkit', 'Moz', 'O'], o = e.charAt(0).toUpperCase() + e.slice(1), n = 0; n < t.length - 1; n++) {
      let i = t[n],
        r = i ? `${i}${o}` : e;
      if (typeof window.document.body.style[r] !== 'undefined') return r;
    }
    return null;
  }

  function D() {
    return this.state.isDestroyed = !0, W(this.modifiers, 'applyStyle') && (this.popper.removeAttribute('x-placement'), this.popper.style.left = '', this.popper.style.position = '', this.popper.style.top = '', this.popper.style[B('transform')] = ''), this.disableEventListeners(), this.options.removeOnDestroy && this.popper.parentNode.removeChild(this.popper), this;
  }

  function H(e, t, o, i) {
    let r = e.nodeName === 'BODY',
      p = r ? window : e;
    p.addEventListener(t, o, {
      passive: !0,
    }), r || H(n(p.parentNode), t, o, i), i.push(p);
  }

  function P(e, t, o, i) {
    o.updateBound = i, window.addEventListener('resize', o.updateBound, {
      passive: !0,
    });
    const r = n(e);
    return H(r, 'scroll', o.updateBound, o.scrollParents), o.scrollElement = r, o.eventsEnabled = !0, o;
  }

  function A() {
    this.state.eventsEnabled || (this.state = P(this.reference, this.options, this.state, this.scheduleUpdate));
  }

  function M(e, t) {
    return window.removeEventListener('resize', t.updateBound), t.scrollParents.forEach((e) => {
      e.removeEventListener('scroll', t.updateBound);
    }), t.updateBound = null, t.scrollParents = [], t.scrollElement = null, t.eventsEnabled = !1, t;
  }

  function I() {
    this.state.eventsEnabled && (window.cancelAnimationFrame(this.scheduleUpdate), this.state = M(this.reference, this.state));
  }

  function R(e) {
    return e !== '' && !isNaN(parseFloat(e)) && isFinite(e);
  }

  function U(e, t) {
    Object.keys(t).forEach((o) => {
      let i = ''; ['width', 'height', 'top', 'right', 'bottom', 'left'].indexOf(o) !== -1 && R(t[o]) && (i = 'px'), e.style[o] = t[o] + i;
    });
  }

  function Y(e, t) {
    Object.keys(t).forEach((o) => {
      const i = t[o];
      !1 === i ? e.removeAttribute(o) : e.setAttribute(o, t[o]);
    });
  }

  function F(e, t, o) {
    let i = T(e, (e) => {
        const o = e.name;
        return o === t;
      }),
      n = !!i && e.some(e => e.name === o && e.enabled && e.order < i.order);
    if (!n) {
      const r = `\`${t}\``;
      console.warn(`\`${o}\`` + ` modifier is required by ${r} modifier in order to work, be sure to include it before ${r}!`);
    }
    return n;
  }

  function j(e) {
    return e === 'end' ? 'start' : e === 'start' ? 'end' : e;
  }

  function K(e) {
    let t = arguments.length > 1 && void 0 !== arguments[1] && arguments[1],
      o = ae.indexOf(e),
      i = ae.slice(o + 1).concat(ae.slice(0, o));
    return t ? i.reverse() : i;
  }

  function q(e, t, o, i) {
    let n = e.match(/((?:\-|\+)?\d*\.?\d*)(.*)/),
      r = +n[1],
      p = n[2];
    if (!r) return e;
    if (p.indexOf('%') === 0) {
      let s;
      switch (p) {
        case '%p':
          s = o;
          break;
        case '%':
        case '%r':
        default:
          s = i;
      }
      const d = c(s);
      return d[t] / 100 * r;
    }
    if (p === 'vh' || p === 'vw') {
      let a;
      return a = p === 'vh' ? _(document.documentElement.clientHeight, window.innerHeight || 0) : _(document.documentElement.clientWidth, window.innerWidth || 0), a / 100 * r;
    }
    return r;
  }

  function G(e, t, o, i) {
    let n = [0, 0],
      r = ['right', 'left'].indexOf(i) !== -1,
      p = e.split(/(\+|\-)/).map(e => e.trim()),
      s = p.indexOf(T(p, e => e.search(/,|\s/) !== -1));
    p[s] && p[s].indexOf(',') === -1 && console.warn('Offsets separated by white space(s) are deprecated, use a comma (,) instead.');
    let d = /\s*,\s*|\s+/,
      a = s === -1 ? [p] : [p.slice(0, s).concat([p[s].split(d)[0]]), [p[s].split(d)[1]].concat(p.slice(s + 1))];
    return a = a.map((e, i) => {
      let n = (i === 1 ? !r : r) ? 'height' : 'width',
        p = !1;
      return e.reduce((e, t) => (e[e.length - 1] === '' && ['+', '-'].indexOf(t) !== -1 ? (e[e.length - 1] = t, p = !0, e) : p ? (e[e.length - 1] += t, p = !1, e) : e.concat(t)), []).map(e => q(e, n, t, o));
    }), a.forEach((e, t) => {
      e.forEach((o, i) => {
        R(o) && (n[t] += o * (e[i - 1] === '-' ? -1 : 1));
      });
    }), n;
  }
  for (var z = Math.min, V = Math.floor, _ = Math.max, X = ['native code', '[object MutationObserverConstructor]'], Q = function (e) {
      return X.some(t => (e || '').toString().indexOf(t) > -1);
    }, J = typeof window !== 'undefined', Z = ['Edge', 'Trident', 'Firefox'], $ = 0, ee = 0; ee < Z.length; ee += 1) {
    if (J && navigator.userAgent.indexOf(Z[ee]) >= 0) {
      $ = 1;
      break;
    }
  }
  var i,
    te = J && Q(window.MutationObserver),
    oe = te ? function (e) {
      let t = !1,
        o = 0,
        i = document.createElement('span'),
        n = new MutationObserver((() => {
          e(), t = !1;
        }));
      return n.observe(i, {
        attributes: !0,
      }),
      function () {
        t || (t = !0, i.setAttribute('x-index', o), ++o);
      };
    } : function (e) {
      let t = !1;
      return function () {
        t || (t = !0, setTimeout(() => {
          t = !1, e();
        }, $));
      };
    },
    ie = function () {
      return void 0 == i && (i = navigator.appVersion.indexOf('MSIE 10') !== -1), i;
    },
    ne = function (e, t) {
      if (!(e instanceof t)) throw new TypeError('Cannot call a class as a function');
    },
    re = (function () {
      function e(e, t) {
        for (var o, n = 0; n < t.length; n++) o = t[n], o.enumerable = o.enumerable || !1, o.configurable = !0, 'value' in o && (o.writable = !0), Object.defineProperty(e, o.key, o);
      }
      return function (t, o, i) {
        return o && e(t.prototype, o), i && e(t, i), t;
      };
    }()),
    pe = function (e, t, o) {
      return t in e ? Object.defineProperty(e, t, {
        value: o,
        enumerable: !0,
        configurable: !0,
        writable: !0,
      }) : e[t] = o, e;
    },
    se = Object.assign || function (e) {
      for (var t, o = 1; o < arguments.length; o++) { for (const i in t = arguments[o], t) Object.prototype.hasOwnProperty.call(t, i) && (e[i] = t[i]); }
      return e;
    },
    de = ['auto-start', 'auto', 'auto-end', 'top-start', 'top', 'top-end', 'right-start', 'right', 'right-end', 'bottom-end', 'bottom', 'bottom-start', 'left-end', 'left', 'left-start'],
    ae = de.slice(3),
    fe = {
      FLIP: 'flip',
      CLOCKWISE: 'clockwise',
      COUNTERCLOCKWISE: 'counterclockwise',
    },
    le = (function () {
      function t(o, i) {
        let n = this,
          r = arguments.length > 2 && void 0 !== arguments[2] ? arguments[2] : {};
        ne(this, t), this.scheduleUpdate = function () {
          return requestAnimationFrame(n.update);
        }, this.update = oe(this.update.bind(this)), this.options = se({}, t.Defaults, r), this.state = {
          isDestroyed: !1,
          isCreated: !1,
          scrollParents: [],
        }, this.reference = o.jquery ? o[0] : o, this.popper = i.jquery ? i[0] : i, this.options.modifiers = {}, Object.keys(se({}, t.Defaults.modifiers, r.modifiers)).forEach((e) => {
          n.options.modifiers[e] = se({}, t.Defaults.modifiers[e] || {}, r.modifiers ? r.modifiers[e] : {});
        }), this.modifiers = Object.keys(this.options.modifiers).map(e => se({
          name: e,
        }, n.options.modifiers[e])).sort((e, t) => e.order - t.order), this.modifiers.forEach((t) => {
          t.enabled && e(t.onLoad) && t.onLoad(n.reference, n.popper, n.options, t, n.state);
        }), this.update();
        const p = this.options.eventsEnabled;
        p && this.enableEventListeners(), this.state.eventsEnabled = p;
      }
      return re(t, [{
        key: 'update',
        value() {
          return k.call(this);
        },
      }, {
        key: 'destroy',
        value() {
          return D.call(this);
        },
      }, {
        key: 'enableEventListeners',
        value() {
          return A.call(this);
        },
      }, {
        key: 'disableEventListeners',
        value() {
          return I.call(this);
        },
      }]), t;
    }());
  return le.Utils = (typeof window === 'undefined' ? global : window).PopperUtils, le.placements = de, le.Defaults = {
    placement: 'bottom',
    eventsEnabled: !0,
    removeOnDestroy: !1,
    onCreate() {},
    onUpdate() {},
    modifiers: {
      shift: {
        order: 100,
        enabled: !0,
        fn(e) {
          let t = e.placement,
            o = t.split('-')[0],
            i = t.split('-')[1];
          if (i) {
            let n = e.offsets,
              r = n.reference,
              p = n.popper,
              s = ['bottom', 'top'].indexOf(o) !== -1,
              d = s ? 'left' : 'top',
              a = s ? 'width' : 'height',
              f = {
                start: pe({}, d, r[d]),
                end: pe({}, d, r[d] + r[a] - p[a]),
              };
            e.offsets.popper = se({}, p, f[i]);
          }
          return e;
        },
      },
      offset: {
        order: 200,
        enabled: !0,
        fn(e, t) {
          let o,
            i = t.offset,
            n = e.placement,
            r = e.offsets,
            p = r.popper,
            s = r.reference,
            d = n.split('-')[0];
          return o = R(+i) ? [+i, 0] : G(i, p, s, d), d === 'left' ? (p.top += o[0], p.left -= o[1]) : d === 'right' ? (p.top += o[0], p.left += o[1]) : d === 'top' ? (p.left += o[0], p.top -= o[1]) : d === 'bottom' && (p.left += o[0], p.top += o[1]), e.popper = p, e;
        },
        offset: 0,
      },
      preventOverflow: {
        order: 300,
        enabled: !0,
        fn(e, t) {
          let o = t.boundariesElement || r(e.instance.popper);
          e.instance.reference === o && (o = r(o));
          const i = w(e.instance.popper, e.instance.reference, t.padding, o);
          t.boundaries = i;
          let n = t.priority,
            p = e.offsets.popper,
            s = {
              primary(e) {
                let o = p[e];
                return p[e] < i[e] && !t.escapeWithReference && (o = _(p[e], i[e])), pe({}, e, o);
              },
              secondary(e) {
                let o = e === 'right' ? 'left' : 'top',
                  n = p[o];
                return p[e] > i[e] && !t.escapeWithReference && (n = z(p[o], i[e] - (e === 'right' ? p.width : p.height))), pe({}, o, n);
              },
            };
          return n.forEach((e) => {
            const t = ['left', 'top'].indexOf(e) === -1 ? 'secondary' : 'primary';
            p = se({}, p, s[t](e));
          }), e.offsets.popper = p, e;
        },
        priority: ['left', 'right', 'top', 'bottom'],
        padding: 5,
        boundariesElement: 'scrollParent',
      },
      keepTogether: {
        order: 400,
        enabled: !0,
        fn(e) {
          let t = e.offsets,
            o = t.popper,
            i = t.reference,
            n = e.placement.split('-')[0],
            r = V,
            p = ['top', 'bottom'].indexOf(n) !== -1,
            s = p ? 'right' : 'bottom',
            d = p ? 'left' : 'top',
            a = p ? 'width' : 'height';
          return o[s] < r(i[d]) && (e.offsets.popper[d] = r(i[d]) - o[a]), o[d] > r(i[s]) && (e.offsets.popper[d] = r(i[s])), e;
        },
      },
      arrow: {
        order: 500,
        enabled: !0,
        fn(e, t) {
          if (!F(e.instance.modifiers, 'arrow', 'keepTogether')) return e;
          let o = t.element;
          if (typeof o === 'string') {
            if (o = e.instance.popper.querySelector(o), !o) return e;
          } else if (!e.instance.popper.contains(o)) return console.warn('WARNING: `arrow.element` must be child of its popper element!'), e;
          let i = e.placement.split('-')[0],
            n = e.offsets,
            r = n.popper,
            p = n.reference,
            s = ['left', 'right'].indexOf(i) !== -1,
            d = s ? 'height' : 'width',
            a = s ? 'top' : 'left',
            f = s ? 'left' : 'top',
            l = s ? 'bottom' : 'right',
            m = O(o)[d];
          p[l] - m < r[a] && (e.offsets.popper[a] -= r[a] - (p[l] - m)), p[a] + m > r[l] && (e.offsets.popper[a] += p[a] + m - r[l]);
          let h = p[a] + p[d] / 2 - m / 2,
            g = h - c(e.offsets.popper)[a];
          return g = _(z(r[d] - m, g), 0), e.arrowElement = o, e.offsets.arrow = {}, e.offsets.arrow[a] = Math.round(g), e.offsets.arrow[f] = '', e;
        },
        element: '[x-arrow]',
      },
      flip: {
        order: 600,
        enabled: !0,
        fn(e, t) {
          if (W(e.instance.modifiers, 'inner')) return e;
          if (e.flipped && e.placement === e.originalPlacement) return e;
          let o = w(e.instance.popper, e.instance.reference, t.padding, t.boundariesElement),
            i = e.placement.split('-')[0],
            n = L(i),
            r = e.placement.split('-')[1] || '',
            p = [];
          switch (t.behavior) {
            case fe.FLIP:
              p = [i, n];
              break;
            case fe.CLOCKWISE:
              p = K(i);
              break;
            case fe.COUNTERCLOCKWISE:
              p = K(i, !0);
              break;
            default:
              p = t.behavior;
          }
          return p.forEach((s, d) => {
            if (i !== s || p.length === d + 1) return e;
            i = e.placement.split('-')[0], n = L(i);
            let a = e.offsets.popper,
              f = e.offsets.reference,
              l = V,
              m = i === 'left' && l(a.right) > l(f.left) || i === 'right' && l(a.left) < l(f.right) || i === 'top' && l(a.bottom) > l(f.top) || i === 'bottom' && l(a.top) < l(f.bottom),
              h = l(a.left) < l(o.left),
              c = l(a.right) > l(o.right),
              g = l(a.top) < l(o.top),
              u = l(a.bottom) > l(o.bottom),
              b = i === 'left' && h || i === 'right' && c || i === 'top' && g || i === 'bottom' && u,
              y = ['top', 'bottom'].indexOf(i) !== -1,
              w = !!t.flipVariations && (y && r === 'start' && h || y && r === 'end' && c || !y && r === 'start' && g || !y && r === 'end' && u);
            (m || b || w) && (e.flipped = !0, (m || b) && (i = p[d + 1]), w && (r = j(r)), e.placement = i + (r ? `-${r}` : ''), e.offsets.popper = se({}, e.offsets.popper, S(e.instance.popper, e.offsets.reference, e.placement)), e = N(e.instance.modifiers, e, 'flip'));
          }), e;
        },
        behavior: 'flip',
        padding: 5,
        boundariesElement: 'viewport',
      },
      inner: {
        order: 700,
        enabled: !1,
        fn(e) {
          let t = e.placement,
            o = t.split('-')[0],
            i = e.offsets,
            n = i.popper,
            r = i.reference,
            p = ['left', 'right'].indexOf(o) !== -1,
            s = ['top', 'left'].indexOf(o) === -1;
          return n[p ? 'left' : 'top'] = r[t] - (s ? n[p ? 'width' : 'height'] : 0), e.placement = L(t), e.offsets.popper = c(n), e;
        },
      },
      hide: {
        order: 800,
        enabled: !0,
        fn(e) {
          if (!F(e.instance.modifiers, 'hide', 'preventOverflow')) return e;
          let t = e.offsets.reference,
            o = T(e.instance.modifiers, e => e.name === 'preventOverflow').boundaries;
          if (t.bottom < o.top || t.left > o.right || t.top > o.bottom || t.right < o.left) {
            if (!0 === e.hide) return e;
            e.hide = !0, e.attributes['x-out-of-boundaries'] = '';
          } else {
            if (!1 === e.hide) return e;
            e.hide = !1, e.attributes['x-out-of-boundaries'] = !1;
          }
          return e;
        },
      },
      computeStyle: {
        order: 850,
        enabled: !0,
        fn(e, t) {
          let o = t.x,
            i = t.y,
            n = e.offsets.popper,
            p = T(e.instance.modifiers, e => e.name === 'applyStyle').gpuAcceleration;
          void 0 !== p && console.warn('WARNING: `gpuAcceleration` option moved to `computeStyle` modifier and will not be supported in future versions of Popper.js!');
          let s,
            d,
            a = void 0 === p ? t.gpuAcceleration : p,
            f = r(e.instance.popper),
            l = g(f),
            m = {
              position: n.position,
            },
            h = {
              left: V(n.left),
              top: V(n.top),
              bottom: V(n.bottom),
              right: V(n.right),
            },
            c = o === 'bottom' ? 'top' : 'bottom',
            u = i === 'right' ? 'left' : 'right',
            b = B('transform');
          if (d = c == 'bottom' ? -l.height + h.bottom : h.top, s = u == 'right' ? -l.width + h.right : h.left, a && b) m[b] = `translate3d(${s}px, ${d}px, 0)`, m[c] = 0, m[u] = 0, m.willChange = 'transform';
          else {
            let y = c == 'bottom' ? -1 : 1,
              w = u == 'right' ? -1 : 1;
            m[c] = d * y, m[u] = s * w, m.willChange = `${c}, ${u}`;
          }
          const v = {
            'x-placement': e.placement,
          };
          return e.attributes = se({}, v, e.attributes), e.styles = se({}, m, e.styles), e;
        },
        gpuAcceleration: !0,
        x: 'bottom',
        y: 'right',
      },
      applyStyle: {
        order: 900,
        enabled: !0,
        fn(e) {
          return U(e.instance.popper, e.styles), Y(e.instance.popper, e.attributes), e.offsets.arrow && U(e.arrowElement, e.offsets.arrow), e;
        },
        onLoad(e, t, o, i, n) {
          let r = x(n, t, e),
            p = E(o.placement, r, t, e, o.modifiers.flip.boundariesElement, o.modifiers.flip.padding);
          return t.setAttribute('x-placement', p), U(t, {
            position: 'absolute',
          }), o;
        },
        gpuAcceleration: void 0,
      },
    },
  }, le;
}));
